# Inventory System — production-grade rewrite (PySide6 + SQLAlchemy +
# PostgreSQL), replacing the legacy Tkinter/Excel app incrementally. See
# docs/architecture.md. This file is self-contained: the legacy app at the
# repo root has its own requirements.txt and does not need any of this.
#
# Needed right now (Phase 0/1 code actually imports these):
PySide6>=6.7
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
pytest>=8.0

# Needed once Phase 2 (SQLAlchemy/PostgreSQL) is implemented:
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
argon2-cffi>=23.1

# Reporting (app/reporting/export.py): CSV/Excel export. Charts, PDF
# export, and Print use PySide6's own QtCharts/QtPdf/QtPrintSupport —
# already covered by the PySide6 line above, no separate package needed.
pandas>=2.2
openpyxl>=3.1

# Invoice PDF generation (app/reports/sales_invoice_pdf.py):
reportlab>=4.1

# Test-only: PDF text extraction for tests/reports/test_sales_invoice_pdf.py.
pypdf>=5.0
