"""Business logic: pure calculation/validation rules, no I/O, no framework.

Everything here takes and returns plain values or dataclasses (Decimal for
money) so it can be unit tested without a GUI, a repository, or a database.
"""
