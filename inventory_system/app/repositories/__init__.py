"""Repositories: one Protocol interface per aggregate (interfaces.py), each
with an excel/ implementation (today, real) and a sql/ implementation
(Phase 2, stubbed). Services depend only on the Protocol — see
docs/architecture.md's migration strategy.
"""
