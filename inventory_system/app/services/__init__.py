"""Application services: one class per use case. Orchestrates app.domain
(pure rules) + app.repositories (persistence). This is the only layer
inventory_app.py will import from once Phase 1 cuts the GUI over — see
docs/architecture.md.
"""
